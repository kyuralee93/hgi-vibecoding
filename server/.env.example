# 로컬(비-Docker) 실행 시 사용. docker-compose 사용 시에는 compose가 주입하므로 불필요.
# .env로 복사 후 값을 채우세요: cp .env.example .env

# 로컬 PostgreSQL 예시
DATABASE_URL="postgresql://gra:gra@localhost:5432/gra"

PORT=3000

# 5단계(AI 연동)에서 추가할 키 — 지금은 비워두세요
# ANTHROPIC_API_KEY=
