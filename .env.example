# 이 파일을 .env 로 복사해서 값을 채우세요: cp .env.example .env
# docker compose 가 자동으로 읽어 server 컨테이너에 주입합니다.

# AI(Claude) — 비워두면 AI 기능은 비활성(키 없음 안내), 키를 넣으면 실제 Claude 호출
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-sonnet-4-6

# 운영 시 반드시 변경
POSTGRES_PASSWORD=gra
JWT_SECRET=gra-dev-secret-change-me
