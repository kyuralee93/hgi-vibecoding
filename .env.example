# 루트 .env — docker compose가 자동으로 읽어 server 컨테이너에 전달합니다.
# 복사 후 키를 채우세요:  cp .env.example .env
# (.env 는 git에 커밋되지 않습니다)

# Claude API 키 (https://console.anthropic.com 에서 발급)
ANTHROPIC_API_KEY=

# 사용할 모델 (기본: claude-opus-4-8). 비용 절감 시 claude-haiku-4-5 등
AI_MODEL=claude-opus-4-8
