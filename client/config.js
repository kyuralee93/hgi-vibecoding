/* ===================================================================
 * config.js — AI 설정 (배포판)
 * -------------------------------------------------------------------
 * 이 배포판은 서버(/api/ai)를 통해 Claude를 호출합니다.
 * API 키는 서버 환경변수(ANTHROPIC_API_KEY)에만 두며, 이 파일에는
 * 절대 키를 넣지 않습니다. (프론트엔드로 키를 내려보내지 않음)
 * =================================================================== */
window.GRA_CONFIG = {
  // 배포판: 브라우저 직접호출 금지. 항상 백엔드(/api/ai) 경유.
  useDirect: false,

  // 모델은 서버의 ANTHROPIC_MODEL 환경변수로 관리합니다. (여기 값은 미사용)
  model: "claude-sonnet-4-6",

  // 키는 서버에만 존재. 프론트엔드에는 두지 않습니다.
  anthropicApiKey: "",
};
